-- this is a configuration for lya
-- some of these settings are used before the webview window is created
-- so that some information can be ready beforehnd
-- instead of being set after the window is already created (delayed)
return {
    title = "Lya Application",
    size = { 800, 600 }
}
